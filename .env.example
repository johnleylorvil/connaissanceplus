DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=konesans_plus

# Required for the pedagogical tutor. Keep the API key on the backend only.
OPENAI_API_KEY=
OPENAI_MODEL=

# Docker Compose local LiveKit credentials.
# Use the same values in backend/.env.
LIVEKIT_API_KEY=devkey
LIVEKIT_API_SECRET=devsecret